@using BlazorNotesApp.Model;
@foreach (var item in @Notes)
{
	var hrefinfo = "/Note/" + @item.Id;
	<MudNavLink Href=@hrefinfo Match="NavLinkMatch.Prefix">@(string.IsNullOrEmpty(item.Name) ? "Untitled name" : item.Name)</MudNavLink>
}

